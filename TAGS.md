# Tags

## Intended Research Tag

```text
v0.1-bios-research
```

Target purpose:

```text
First complete DBYTE A780L3B BIOS 95W research archive snapshot.
```

Canonical release note:

```text
RELEASE.md
releases/v0.1-bios-research.md
```

## Current Limitation

The repository includes an Actions workflow to create the GitHub Release automatically:

```text
.github/workflows/create-v0.1-release.yml
```

However, GitHub Actions execution is disabled on the account due to billing lock, so the actual GitHub tag/release page cannot be generated by workflow until account-level Actions execution is restored.
